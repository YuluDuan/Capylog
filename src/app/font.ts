import { Inter } from "next/font/google";

export const inter = Inter({
    weight: ['700'],
    subsets: ["latin"],
    display: "swap",
});
export const interR = Inter({
    weight: ['300'],
    subsets: ["latin"],
    display: "swap",
});